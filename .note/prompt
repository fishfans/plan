# 出初始设计
1. 只需要一个html文件
2. 数据存储使用json文件
3. json 里面存储的数据就是 plan set、plan item，item tag 以及
它们之间的关系
4. 一个plan set里面可以包含多个plan item，放在一个矩形框里面，
随着plan item的增加会上下变长。矩形框左上角是一个三横的图标，
点击上下拖可以改变plan set矩形框的顺序,右上角是一个叉，点击会
删除整个矩形框，这两个按钮都是当鼠标悬浮时出现，平时隐藏。
居中的位置可以变价plan set标题
5. plan item 位于 plan set矩形框里面，每个占据一行，也会随内容
大小扩充，但右上角会有两个小按钮，下三角点击可以选择预先设置
好的item tag。plan item的item tag紧跟着内容文字其后，用预设颜色
字体放在小框里显示。右上角另外一个叉按钮点击删除对应plan item。
双击plan item可以直接编辑内容
6.点击 create 按钮会创建一个默认plan set和其中的两个默认plan item，此时
create 按钮会变成了export按钮
7. 点击import按钮会打开本地文件目录选择器让用户选择导入的json文件，然后
下方显示的也就是json文件里面的plan set、plan item，item tag了，所有操作
最后会应用到这个json文件里面。此时create 按钮也会变成export按钮
8. 这个html是按照日期分页的，最下方会显示不同日期按钮，点击可以切换
9.点击export按钮会下载一个压缩包，压缩包里面有存储数据的json文件plandata.json
和一个图片目录pics，pics里面放的是每个日期页面的图片png。也可以选择仅导出plandata.json。
10. 点击+plan set会在当前页面最前面新增一个plan set
11. +plan item平常是灰色的，只有当点击某个plan set选中plan set之后才会变亮，此时
点击会在该plan set 最前面新增一个plan item
12. 点击+ item tag会弹窗显示目前已经有的tag和对应的字体颜色，然后可以在弹窗新增
新的tag和对应的字体颜色。
13.当用户要关闭此浏览器窗口要警告提示用户出数据，确认后才关闭
14. 保留这种手绘风格


# 有以下问题
1. 我希望不是第一个的plan set也是和第一个一样样式是黄色的。
2. +plan set, +plan Item, +item tag 按钮还是希望放在页面顶部这里
3. 希望日期选择除了前一个后一个按钮，也可以直接编辑日期修改当前的分页日期
4. 导出的时候，如果没有任何数据即没有任何plan set，就不要生成图片
5. 增加一个按钮在日期切换旁边，就是copy to next day 按钮，会把当前页面的数据拷贝到下一天的页面
6. 选择标签的下拉框位置不对，应该紧跟在点击的下三角下面 
7. 页面标题改为 another gift for you

# 再优化
1. 截图时候把上下的按钮组件部分也可以截图下来，因为希望看到上面的标题和下面的日期
2. 底部再添加一个按钮 changdate 可以直接修改当前页面的日期为输入的日期。注意不仅仅是切换到该日期，数据也将剪切到此日期页面的效果
3. plan set 的容器不要使用滑动的列表，上下铺开展示
4. 离开此网站的警告语改为 "注意确认已把编辑的内容导出!离开后系统不会保留你的更改!"
5. 标签的展示嵌入到plan item文字末尾和最后一行文字是同一行
6. 页面标题改为 another git for 温小声 by berlinlancho

# 微调
1. change date 里面按钮文字使用英文，剪切解释删除
2. 标题改为 Another git for 温小声 by berlinlancho
3. plan set 的颜色调整地像可爱的肉色那样子有一些淡淡的粉


# bug
1. 电脑网页端时顶部和底部的按钮和标题都保持在一行里面
2. 手机页面端时里面的按钮就可以换行来保持按钮大小不变
手机端的展示还是要改一下，改成可以上下换行的. 还有plan itme后面的标签应该和最后的文字间隔两个空格的距离

# 需要绑定一些快捷键
1. 点击选中某个plan item时 回车可以在当前plan set 新增一个plan Item
2. 左右方向键对象日期的prev 和 next 两个按钮


# 重构
1. 不要把所有的代码都放在一个html文件里面了，新建一个js的文件夹，把里面的功能js代码分成多个模块文件。
3. 删除 +Plan itme 按钮。在每个plan set 的左上角，汉堡按钮的右边加一个 加号按钮，点击会在对应的plan set最下方新增一个plan item。需要优化一下
回车新增plan item，应该是点击到某个plan item的时候回车，是直接在它的下方新增一个新的 plan Item
4. 我发现一个问题，就是最后一个plan set 中plan item选择标签的时候因为标签下拉框较长导致显示不全，我希望当标签下拉框显示要超出页面长度的时候位置可以上移达到
完整显示的目的
5. 删除create 符号，点击+plan set就相当于开始创建了，提示语也可以修改一下。+plan set 符号和 + item tag符号应该放在左边，import 和 export按钮放在右边

# 存储模块
1. 需要按照 git_as_remote_db 里面demo那样，把github对应的仓库当作远程的数据存储目录！这个项目的存储需要发生较大的变化。增加两个按钮save 和submit。当导入或者
创建之后，数据存储在浏览器localstorage里面，7天过期，点击save会把更新的数据保存到localstorage，点击submit就会git到对应的仓库路径。import文件之后也需要点击save
才可存到浏览器localstorage里面。在底部最左边放上一个设置按钮，点击会打开新页面配置GitHub相关需要配置的参数，配置好可以测试连接就像demo里面一样，不同的是这些配置好的信息
存储在js/config.json里面，后面打开也是直接从这个文件里面读取。不过存储到js/config.json的数据是加密的，每次用户输入配置好信息，测试成功后让让他提供一个密码进行加密。
然后当点击submit要和github仓库交互的时候就需要弹窗用户输入密码(当然只需要输入一次，存储到内存里面，再提交就不会弹窗)，程序就使用密码解密json文件里面的数据，然后就可以
和GitHub仓库交互。每次新打开页面的时候也是弹窗输入密码，以加载github仓库里面的内容。当然用户也可以直接关闭，离线的使用import和export。